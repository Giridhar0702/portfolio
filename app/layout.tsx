import type React from "react"
import type { Metadata } from "next"
import { Inter, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _inter = Inter({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Giridhar S K | Full-Stack Developer",
  description:
    "Computer Science & Engineering Undergraduate | Full-Stack Developer passionate about building production-ready web applications with clean design and scalable systems.",
  keywords: ["Full-Stack Developer", "Web Developer", "React", "Next.js", "Computer Science"],
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
